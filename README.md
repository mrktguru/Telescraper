# Telegram Comments Parser 🚀

Простой и мощный парсер комментариев из Telegram каналов с красивым CLI интерфейсом.

## Возможности

- ✅ Парсинг комментариев из публичных Telegram каналов
- ✅ Красивый терминальный интерфейс с таблицами и прогресс-барами
- ✅ Экспорт в CSV с поддержкой кириллицы
- ✅ Фильтрация ботов и дубликатов
- ✅ Гибкие настройки через CLI опции
- ✅ Защита от rate limits
- ✅ Обработка ошибок и retry логика

## Требования

- Python 3.10 или выше
- Telegram аккаунт
- API credentials от Telegram

## Установка

### 1. Клонируйте репозиторий

```bash
git clone <repository-url>
cd Telescraper
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Получите Telegram API credentials

1. Перейдите на https://my.telegram.org/auth
2. Войдите с вашим номером телефона
3. Перейдите в раздел "API development tools"
4. Создайте новое приложение
5. Сохраните `api_id` и `api_hash`

### 4. Настройте .env файл

```bash
cp .env.example .env
```

Отредактируйте `.env` файл и добавьте свои данные:

```env
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash
TELEGRAM_PHONE=+1234567890

CHANNEL_URL=https://t.me/okkosport
POSTS_LIMIT=30
OUTPUT_DIR=./output
```

## Использование

### Базовые команды

```bash
# Парсинг с настройками по умолчанию (30 постов, первые 10 результатов)
python parser.py

# Показать все результаты в терминале
python parser.py --show-all

# Парсить больше постов
python parser.py --posts 50
python parser.py --posts 100

# Парсить другой канал
python parser.py --channel https://t.me/another_channel

# Тихий режим (только итоги, без деталей по каждому посту)
python parser.py --quiet

# Не сохранять CSV файл
python parser.py --no-csv

# Комбинация опций
python parser.py --posts 100 --show-all --quiet
```

### Опции командной строки

| Опция | Описание | По умолчанию |
|-------|----------|--------------|
| `--posts N` | Количество постов для парсинга | 30 |
| `--show-all` | Показать все результаты в терминале | первые 10 |
| `--channel URL` | URL канала для парсинга | https://t.me/okkosport |
| `--quiet` | Минимальный вывод (без деталей по постам) | выключен |
| `--no-csv` | Не сохранять CSV файл | сохранять |
| `--help` | Показать справку | - |

## Примеры вывода

### Обычный запуск

```
╔══════════════════════════════════════════════════════════╗
║         🚀 Telegram Comments Parser v1.0                 ║
╚══════════════════════════════════════════════════════════╝

📡 Connecting to Telegram...
✓ Authenticated as: +1234567890

🎯 Target: https://t.me/okkosport
📊 Posts to check: 30

Parsing posts... ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 30/30 100%

Post #12345: ✓ 15 comments
Post #12346: - no comments
Post #12347: ✓ 8 comments
...

⏱️  Completed in 45 seconds

╔══════════════════════════════════════════════════════════╗
║                    PARSING RESULTS                       ║
╠══════════════════════════════════════════════════════════╣
║ Total posts checked:        30                           ║
║ Posts with comments:        12                           ║
║ Total comments found:       156                          ║
║ Unique users collected:     89                           ║
║ Bots filtered out:          0                            ║
╚══════════════════════════════════════════════════════════╝

┏━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━━┳━━━━━━━━━━━━┳━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ First Name    ┃ Username      ┃ User ID    ┃ Comment (first 50 chars)       ┃
┡━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━━━╇━━━━━━━━━━━━╇━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┩
│ Алексей       │ alex_runner   │ 123456789  │ Отличные кроссовки! Где купит… │
│ Мария         │ -             │ 987654321  │ Согласна, качество супер       │
│ Дмитрий       │ dmitry_sport  │ 456789123  │ А есть в размере 43?           │
│ ...           │ ...           │ ...        │ ...                            │
└───────────────┴───────────────┴────────────┴────────────────────────────────┘

💡 Showing first 10 of 89 users.
   Run with --show-all to see all results

💾 Full data saved to: ./output/okkosport_commenters_2024-11-26_15-30-00.csv

📝 Options:
  --show-all    Show all users in terminal
  --posts N     Parse N posts (default: 30)
  --no-csv      Don't save CSV file
  --quiet       Minimal output
```

## Структура проекта

```
Telescraper/
├── .env                    # API credentials (не в git)
├── .env.example           # Пример конфигурации
├── parser.py              # Основной скрипт
├── requirements.txt       # Python зависимости
├── README.md             # Документация
├── telegram_comments_parser_spec.md  # Техническая спецификация
├── session.session       # Telegram сессия (создается автоматически)
└── output/               # CSV файлы с результатами
    └── okkosport_commenters_2024-11-26_15-30-00.csv
```

## Формат CSV

CSV файлы сохраняются с кодировкой `utf-8-sig` для корректного отображения кириллицы в Excel.

**Колонки:**
- `first_name` - Имя пользователя
- `username` - Username (без @), или `-` если отсутствует
- `user_id` - Уникальный Telegram ID
- `comment_text` - Текст комментария

**Пример:**
```csv
first_name,username,user_id,comment_text
Иван,ivan_petrov,123456789,"Отличный пост!"
Мария,,987654321,"Согласна полностью"
Петр,petr_sport,456789123,"Когда следующий выпуск?"
```

## Обработка ошибок

Парсер корректно обрабатывает:

- ✅ **FloodWaitError** - автоматическое ожидание при rate limit
- ✅ **ChannelPrivateError** - ясное сообщение об ошибке
- ✅ **Deleted accounts** - пропуск удаленных пользователей
- ✅ **Network errors** - retry логика с экспоненциальной задержкой
- ✅ **Посты без комментариев** - корректная обработка

## Защита от Rate Limits

Парсер автоматически:
- Ждет 1 секунду между постами с комментариями
- Ждет 2 секунды для постов с >100 комментариями
- Обрабатывает FloodWaitError и ждет указанное API время
- Использует retry логику (до 3 попыток)

## Фильтрация

Автоматическая фильтрация:
- 🤖 Боты (is_bot check)
- 👥 Дубликаты пользователей (по user_id, сохраняется первый комментарий)
- 🗑️ Удаленные аккаунты

## Первый запуск

При первом запуске Telegram попросит ввести код подтверждения:

```bash
python parser.py
```

1. Код будет отправлен в ваш Telegram
2. Введите код в терминале
3. Создастся файл `session.session` для будущих запусков
4. Больше не потребуется вводить код (пока не удалите session файл)

## Требования к каналу

- Канал должен быть **публичным** или вы должны быть **подписаны** на него
- У канала должны быть **включены комментарии**
- Ваш аккаунт должен иметь **доступ** к комментариям

## Устранение проблем

### "Missing credentials in .env file"
Создайте `.env` файл и добавьте API credentials

### "Channel is private"
- Убедитесь что канал публичный
- Или подпишитесь на канал через свой аккаунт

### "Rate limit hit"
Парсер автоматически обработает - просто подождите

### "FloodWaitError"
Telegram временно ограничил запросы - парсер автоматически подождет и продолжит

## Лицензия

MIT

## Автор

Created with ❤️ for parsing Telegram comments

## Поддержка

Для вопросов и проблем создайте issue в репозитории.

---

**Важно:** Используйте парсер ответственно и соблюдайте правила использования Telegram API.
