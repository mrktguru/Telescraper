# Руководство по просмотру логов

## 🔍 Способы диагностики

### 1. Быстрая проверка системы
```bash
python3 check_logs.py
```
Показывает состояние БД, файлов и переменных окружения.

---

### 2. Запуск веб-приложения с логами в терминале

#### Вариант A: Режим разработки (рекомендуется для отладки)
```bash
python3 web_app.py
```

Вы увидите:
- ✅ Запуск Flask сервера
- 📝 Все HTTP запросы
- ❌ Ошибки Python в реальном времени
- 🔍 Traceback для отладки

**Откройте в браузере:** http://localhost:5000

#### Вариант B: С помощью скрипта
```bash
./run_debug.sh
```

---

### 3. Проверка базы данных напрямую
```bash
python3 << 'EOF'
import sqlite3
conn = sqlite3.connect('data/app.db')
conn.row_factory = sqlite3.Row
cursor = conn.cursor()

# Последние задачи
cursor.execute('SELECT * FROM tasks ORDER BY created_at DESC LIMIT 5')
for task in cursor.fetchall():
    print(dict(task))
    print('-' * 80)

conn.close()
EOF
```

---

### 4. Просмотр логов в веб-интерфейсе

1. Запустите приложение: `python3 web_app.py`
2. Откройте http://localhost:5000
3. Зарегистрируйтесь/войдите
4. Перейдите в раздел **"История"**
5. Там видны все задачи и их ошибки

---

### 5. Логи браузера (JavaScript ошибки)

1. Откройте веб-интерфейс
2. Нажмите **F12** (DevTools)
3. Вкладка **Console** - ошибки JavaScript
4. Вкладка **Network** - HTTP запросы и ответы

---

## 🐛 Типичные ошибки

### Ошибка: "Missing credentials"
```
❌ Error: Missing credentials in .env file
```
**Решение:** Проверьте `.env` файл, должны быть заполнены:
- TELEGRAM_API_ID
- TELEGRAM_API_HASH
- TELEGRAM_PHONE
- SECRET_KEY

### Ошибка: "Authentication failed"
```
❌ Authentication failed: ...
```
**Решение:**
1. Удалите файл `.session`
2. Запустите заново
3. Введите код из Telegram

### Ошибка: "Channel not found"
```
❌ Error: Cannot find entity for channel
```
**Решение:**
- Проверьте URL канала
- Убедитесь, что вы подписаны на канал
- Канал должен быть публичным или вы должны быть участником

---

## 📊 Структура логов

При запуске с `python3 web_app.py` вы увидите:

```
* Running on http://0.0.0.0:5000
* Debugger is active!

127.0.0.1 - - [28/Nov/2025 14:15:32] "GET / HTTP/1.1" 200 -
127.0.0.1 - - [28/Nov/2025 14:15:35] "POST /api/parse HTTP/1.1" 200 -

[Если ошибка:]
Traceback (most recent call last):
  File "web_app.py", line 87, in parse
    ...
Error: [детали ошибки]
```

---

## 💡 Полезные команды

```bash
# Проверка запущенных процессов
ps aux | grep python

# Остановка веб-сервера (Ctrl+C в терминале или)
pkill -f web_app.py

# Очистка базы данных (ОСТОРОЖНО!)
rm data/app.db
python3 -c "from database import init_database; init_database()"

# Просмотр последних строк лога (если логируется в файл)
tail -f app.log

# Проверка портов
netstat -tulpn | grep :5000
```

---

## 🚀 Production логи (если используете systemd)

```bash
# Просмотр логов сервиса
sudo journalctl -u telescraper -f

# Последние 100 строк
sudo journalctl -u telescraper -n 100

# С фильтром по времени
sudo journalctl -u telescraper --since "1 hour ago"

# Статус сервиса
sudo systemctl status telescraper
```

---

## 📝 Добавление логирования в файл (опционально)

Если хотите логи в файл, добавьте в начало `web_app.py`:

```python
import logging

logging.basicConfig(
    filename='app.log',
    level=logging.DEBUG,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)

app.logger.addHandler(logging.FileHandler('app.log'))
```

Затем в коде используйте:
```python
app.logger.info("Parsing started")
app.logger.error(f"Error: {e}")
```
